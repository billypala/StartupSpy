# StartupSpy 简体中文汉化报告

> 项目：https://github.com/Arman-Ispiryan/StartupSpy（已 fork 至 `billypala/StartupSpy-zh-CN`）
> 技术栈：C# 12 / .NET 8 / WPF（MVVM），无第三方 UI 库
> 构建验证：Debug 与 Release 均 **0 错误**通过

---

## 一、项目结构与技术栈分析

| 项 | 说明 |
|----|------|
| 类型 | Windows 桌面程序（WinExe，WPF） |
| 目标框架 | `net8.0-windows`（需 .NET 8 SDK + Windows Desktop 负载） |
| 架构 | MVVM：Models / ViewModels / Services / Converters + XAML 视图 |
| 依赖 | `TaskScheduler` 2.11.1、`System.ServiceProcess.ServiceController` 8.0.0 |
| 用户可见文本位置 | 全部为**内联字符串**（无 `.resx` 资源文件），分布在 XAML 与 C# 中 |

### 包含用户可见文本的文件（共 8 个）
- `MainWindow.xaml` — 全部界面文案（标题、按钮、菜单/筛选、列头、状态栏、空态、详情面板、提示占位符）
- `ViewModels/MainViewModel.cs` — 状态栏文本、扫描/筛选提示、MessageBox 消息
- `Models/StartupEntry.cs` — 类别/风险标签、延迟默认值
- `Services/StartupScannerService.cs` — 发布者兜底值、风险判定哨兵值、服务延迟值
- `Converters/Converters.cs` — 启用/禁用文本转换器
- `App.xaml.cs` — 崩溃对话框标题
- `README.md` — 项目文档
- `installer.iss` — Inno Setup 安装脚本中的用户可见字符串

---

## 二、统一术语表（核心）

| 英文（原文） | 简体中文 | 备注 |
|---|---|---|
| StartupSpy | StartupSpy | **产品名，保留英文** |
| Windows Startup Manager | Windows 启动项管理器 | 副标题 |
| Scan / Scan System | 扫描 / 扫描系统 | |
| Search | 搜索 | |
| by name, publisher, path | 按名称、发布者、路径 | 搜索占位符 |
| TOTAL ENTRIES | 启动项总数 | |
| HIGH RISK | 高风险 | |
| UNKNOWN | 未知 | |
| SAFE | 安全 | |
| Show High Risk | 显示高风险 | |
| Scanning… | 正在扫描… | |
| FILTER | 筛选 | |
| All / Registry / Startup Folder / Scheduled Task(s) / Service | 全部 / 注册表 / 启动文件夹 / 计划任务 / 服务 | |
| NAME / PUBLISHER / RISK / STATUS / PATH | 名称 / 发布者 / 风险 / 状态 / 路径 | 列表列头 |
| No entries found | 未找到启动项 | 空态 |
| Select an entry / to view details | 选择一项 / 查看详情 | 未选中提示 |
| RISK LEVEL | 风险等级 | |
| Category / Location / Command / Delay | 类别 / 位置 / 命令 / 延迟 | 详情字段 |
| DESCRIPTION / ACTIONS | 描述 / 操作 | |
| Disable at Startup / Enable at Startup | 禁用开机启动 / 启用开机启动 | |
| Open Location / Copy Path | 打开位置 / 复制路径 | |
| Enabled / Disabled | 已启用 / 已禁用 | |
| Safe / Low / Medium / High / Unknown（风险） | 安全 / 低 / 中 / 高 / 未知 | |
| None（延迟） / On Boot | 无 / 开机时 | |
| Run as Administrator | 以管理员身份运行 | |
| Create a desktop shortcut / Additional icons | 创建桌面快捷方式 / 附加图标： | 安装脚本 |
| Launch StartupSpy | 启动 StartupSpy | 安装脚本 |
| Crash | 崩溃 | 崩溃对话框 |

**保留英文的专有名词/标识符**：StartupSpy（产品名）、HKLM/HKCU、Registry Run 键、CSV/HTML、VirusTotal、C# 12、.NET 8、WPF、MVVM、TaskScheduler、WMI/Win32 API、KnownSafePublishers（Microsoft/Google/Adobe 等发布者名）、HighRiskKeywords（updater/agent 等逻辑关键字）、所有注册表键/文件路径/配置键/变量名/函数名/API 字段/环境变量。

---

## 三、关键改动说明

### 1. 仅替换展示文本，逻辑/标识符不变
- 变量名、函数名、枚举（`StartupCategory`/`RiskLevel`）、API 字段、配置键、环境变量、第三方库调用**全部保持原样**。
- 所有面向用户的字符串（界面、按钮、菜单、标签、占位符、提示、错误/通知、标题、meta、安装脚本、文档）均已翻译。

### 2. 逻辑耦合处理（保证筛选/风险判定不被破坏）
- **分类筛选键一致性**：`CategoryLabel`（模型）、`Categories` 列表、`MainWindow.xaml` 中 RadioButton 的 `Tag`（筛选键值）三者已**同步中文化**；特殊的 `"All"` 过滤键在所有位置保持一致（`SelectedCategory != "All"`、`SelectedCategory = "All"`、All 按钮 `Tag="All"`），确保 `CategoryLabel == SelectedCategory` 比较与筛选逻辑正确。
- **"未知"哨兵值一致性**：`GetFilePublisher` 三处返回 `"未知"`，与 `AssessRisk` 中的 `publisher == "未知"` 判定保持一致，发布者无法识别时既正确显示为"未知"，也能正确归入"未知"风险。

### 3. 中文相关适配
- **字符编码**：所有含中文的源文件以 UTF-8 保存；`MainWindow.xaml` 顶部显式添加 `<?xml version="1.0" encoding="utf-8"?>` 声明，确保 XAML 标记编译正确。
- **字体**：`MainWindow.xaml` 的 `FontFamily` 由 `Segoe UI` 调整为 `Segoe UI, Microsoft YaHei`（微软雅黑），保证中文在 Windows 下使用合适的 CJK 字体回退渲染。
- **日期/数字/货币**：本项目不展示日期、货币或需本地化的数字格式（仅显示整数计数与文件路径），故无需额外格式化处理。
- **布局溢出**：中文文案普遍比英文更紧凑；列表与详情面板已使用 `TextTrimming="CharacterEllipsis"` 与 `TextWrapping="Wrap"` 防溢出；统计卡片/列宽（如 130px、90px）足以容纳中文标签。

### 4. 构建校验
- 本地原仅装 .NET 2.1 SDK，已通过官方 `dotnet-install.ps1` 安装 **.NET 8 SDK 8.0.424**（用户目录 `~/.dotnet`）。
- `dotnet build -c Debug` 与 `dotnet build -c Release` 均 **成功，0 错误**。
- 现存 3 条 `NU1603` 警告为**项目原有**（指定的 `TaskScheduler 2.11.1` 在源上不可达，自动回退到 2.12.0），与本次汉化无关。

---

## 四、汉化文件清单

| 文件 | 改动类型 |
|------|----------|
| `MainWindow.xaml` | 全部界面文案 + UTF-8 声明 + 中文字体回退 |
| `ViewModels/MainViewModel.cs` | 状态/提示/消息文本、类别列表、筛选键 |
| `Models/StartupEntry.cs` | 类别/风险标签、延迟默认值 |
| `Services/StartupScannerService.cs` | 发布者兜底值、风险哨兵值、服务延迟值 |
| `Converters/Converters.cs` | 启用/禁用文本转换器 |
| `App.xaml.cs` | 崩溃对话框标题 |
| `README.md` | 文档全文汉化（技术令牌保留） |
| `installer.iss` | 安装脚本用户可见字符串 |

本地提交：`edbb492 汉化：将 StartupSpy 完整本地化为简体中文（界面/提示/文档/安装脚本）`

---

## 五、推送状态与后续

✅ **已成功推送到 GitHub fork**：重新链接 GitHub 后，Git 凭据管理器（GCM）已获得写入权限，`git push fork main` 执行成功。

- 提交：`edbb492 汉化：将 StartupSpy 完整本地化为简体中文（界面/提示/文档/安装脚本）`
- 目标分支：`billypala/StartupSpy-zh-CN` 的 `main`（已验证远程 HEAD = `edbb49225f8ae359d8f43256f763735240a39448`）
- 该提交包含上述全部 8 个汉化文件。

后续可选：
- 如需向上游 `Arman-Ispiryan/StartupSpy` 提 PR，请在 GitHub 网页端从您的 fork 发起（本地 `origin` 指向上游，`fork` 指向您的 fork）。
- 如需出安装包，可在 Windows 上用 Inno Setup 打开 `installer.iss` 编译，或在仓库根目录运行：
  ```bash
  dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true
  ```
